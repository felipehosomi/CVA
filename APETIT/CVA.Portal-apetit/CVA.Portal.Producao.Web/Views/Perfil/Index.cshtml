@model List<CVA.Portal.Producao.Model.Configuracoes.PerfilModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <div class="row">
        <div class="x_panel">
            <a href="@Url.Action("Create", "Perfil")">
                <button type="button" class="btn btn-primary pull-right">Novo <span class="fa fa-floppy-o fa-right"></span></button>
            </a>
            <div class="x_title">
                <h2>Perfil</h2>
                <div class="clearfix"></div>
            </div>
            
                <table id="dataTable" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Descrição</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (var i = 0; i < Model.Count; i++)
                        {
                            <tr>
                                <td>@Model[i].Descricao</td>
                                <td style="text-align: center">
                                    <a href='@Url.Action("Edit", new { id = Model[i].Code})' class="btn btn-default btn-small no-margin" data-toggle="tooltip" title="Editar">
                                        <span class="fa fa-pencil"></span>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <button type="button" class="btn btn-default pull-left" onclick="location.href='@Url.Action("Index", "Usuario")'"><span class="fa fa-reply fa-right"></span> Voltar</button>
        </div>
    </div>
</div>